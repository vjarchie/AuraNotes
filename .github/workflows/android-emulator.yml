# Smoke-test the Capacitor Android build on a real AVD (GitHub-hosted Linux + KVM).
# This machine has no Android SDK; run this workflow from the Actions tab (workflow_dispatch) or push to main.

name: Android emulator smoke test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: android-emulator-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  emulator:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            android-app/package-lock.json

      - name: Install dependencies
        run: |
          npm ci --legacy-peer-deps
          (cd android-app && npm ci)

      - name: Build web and Capacitor sync
        run: |
          npm run build:web
          (cd android-app && npx cap sync)

      - name: Make Gradle wrapper executable
        run: chmod +x android-app/android/gradlew

      - name: Emulator — assemble, install, launch app
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          target: google_apis
          disable-animations: true
          # Extra space + software GPU avoids some adb install failures (exit 224) on CI
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -partition-size 8192
          script: |
            set -e
            adb wait-for-device
            booted=""
            until [ "$booted" = "1" ]; do
              booted=$(adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\r' || echo 0)
              sleep 2
            done
            adb uninstall com.aurnotes.app 2>/dev/null || true
            cd android-app/android
            ./gradlew assembleDebug installDebug --no-daemon --stacktrace
            adb devices -l
            adb shell am start -n com.aurnotes.app/.MainActivity || true
            echo "OK: debug APK built and installed on the CI emulator"
